version: "3.9"

services:
  django-web:
    container_name: pdv_project_dev
    volumes:
      - .:/app
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    environment:
      DEBUG: 1
    ports:
      - "8000:8000"
